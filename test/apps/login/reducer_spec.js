import {
	Map, fromJS
}
from 'immutable';
import {
	expect
}
from 'chai';

// import * as reducer from '../../../src/apps/login/reducer';
//
// describe('登录reducer测试', () => {
// 	it('登录成功', () => {
// 		let initialState = Map()
// 		const nextState = reducer.loginSuccess(initialState);
// 		expect(nextState).to.equal(fromJS({
// 			isLogined:true
// 		}));
// 	});
// });
